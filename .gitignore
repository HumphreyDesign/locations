# Only these files are tracked in this repo.
# Everything else is managed locally or via Webflow.
*
!.gitignore
!saracen-locations-*.geojson
# Setup that must survive fresh clones (see CLAUDE.md for why).
!CLAUDE.md
!.mcp.json
